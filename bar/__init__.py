from typing import Dict, List


def foo(baz: dict[str, str]) -> list[str]:
    """Foo something.

    :param baz: The thing to foo.
    :returns: The fooed thing.
    """


def bar(baz: Dict[str, str]) -> List[str]:
    """Bar something."""
